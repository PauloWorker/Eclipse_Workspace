package Jframe_gui;

import java.awt.Color;

import javax.swing.ImageIcon;
import javax.swing.JFrame;

public class Main {
	public static void main(String[] args) {
		
		/*
		JFrame frame = new JFrame(); // Creates a frame
		
		ImageIcon image = new ImageIcon("src/logo.jpg"); //create a image icon
		
		frame.setTitle("JFrame titles goes here"); //sets title of frame
		frame.setSize(420, 420); //sets the x-dimension and y-dimension of frame
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE); //exit out of application, always put this code to close the application instead of hide the window when click in 'x' button to close
		frame.setResizable(false); //prevent to resize the frame
		frame.setVisible(true); //make the frame window visible
		frame.setIconImage(image.getImage()); //change the icon image
		frame.getContentPane().setBackground(new Color(0, 0, 255)); //change the background color
		*/
		
		MyFrame myFrame = new MyFrame();
		
	}
}
